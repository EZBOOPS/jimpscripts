-- Seer's Reach layout variant 1
-- To populate: open the Gem Farmer menu, go to Waypoint Recorder, enable Recording,
-- run from just outside the dungeon entrance all the way to the boss, then stop recording.
-- Click "Print path to console", copy the output, and paste the vec3 lines below.
local path = {
    -- vec3:new(x, y, z),
    -- vec3:new(x, y, z),
}
return path
